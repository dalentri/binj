services:
  db:
    image: postgres:18.3
    container_name: binj_postgres
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: binj_db
    ports:
      - "5432:5432"
